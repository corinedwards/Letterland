{
  "name": "corin-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "ship": "vite build && cd dist && zip -r ../corin-portfolio-build.zip . -x '*.DS_Store' && cd .. && rm -rf dist && echo 'corin-portfolio-build.zip ready'"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "three": "^0.172.0"
  }
}
